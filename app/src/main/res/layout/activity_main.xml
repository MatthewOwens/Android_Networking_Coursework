<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.a1400971example.android_networking_coursework.MainActivity">

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Battle"
        android:id="@+id/battleButton"
        android:layout_above="@+id/tradeButton"
        android:layout_alignParentStart="true"
        android:textColor="#8abc7b" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Trade"
        android:id="@+id/tradeButton"
        android:layout_above="@+id/listButton"
        android:layout_alignParentStart="true"
        android:textColor="#8abc7b" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="List"
        android:id="@+id/listButton"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textColor="#8abc7b" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Romon"
        android:id="@+id/titleText"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textColor="#8abc7b"
        android:textSize="32dp" />

    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:id="@+id/foundImage"
        android:layout_below="@+id/titleText"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/battleButton"
        android:clickable="true"
        android:contextClickable="false" />
</RelativeLayout>
